export type HomepageProduct = {
  id: string;
  name: string;
  slug: string;
  image: string;
  alt: string;
  pricePaise: number;
  sourceUrl: string;
};

export type ServiceItem = {
  id: string;
  title: string;
  body: string;
};
